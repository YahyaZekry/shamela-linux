# Session Log

> Part of shamela-linux/.project-knowledge/ | Last updated: 2026-08-14
> Append-only — never edit past entries.

| Date | Summary |
|------|---------|
| 2026-08-14 | Extracted the PyInstaller-frozen Shamela Linux 1448.2 binary (main `shamela.pyc` + `PYZ-00.pyz` → ~250 pyc), decompiled all 39 app modules with uncompyle6 into `/tmp/opencode/decompiled/`. |
| 2026-08-14 | Created `/tmp/opencode/d3/` — cleaned working tree for the first 8 modules (shamela, updater, options, customs, displaybook, engine, ignore, searchboxes). |
| 2026-08-14 | Fixed all 6 `--- This code section failed: ---` decompilation failures + 2 broken f-strings in `d3/`: `detect_running_arch`, `prepareProgress`, `_settingsSnapshot`, `_changedPreviewKeys`, `realResolutions_new`, `ReadersBrowser.keyPressEvent`, `_buildHtmlFromParagraphs` (×2). All verified byte-identical to the original `.pyc` via `co_code` comparison; whole tree parses clean; zero failure markers remain. |
| 2026-08-14 | Persisted work artifacts into the repo (see history): copied `d3/`, `decompiled/` (39 modules), and `shamela-extract/` (289 pyc) from `/tmp/opencode`, verified integrity. Created `.project-knowledge/`. |
| 2026-08-14 | New goal set: mount the user's Windows Shamela database in the Linux build. Initial research notes added to roadmap. Repo committed + pushed. |
| 2026-08-14 | **DB-mount investigation.** Confirmed it is NOT a mount — the two formats are structurally incompatible → it's a **migration**. Golden shamela = legacy Shamela 3.x: `Books/<id%10>/<id>.mdb` (Jet/Access, tables `book(id,part,page,nass,seal)` + `title(id,lvl,sub,tit)`, text cp1256) + SQLite `book_index.db` catalog (29,278 books, `shamelaID` stable id). Modern Linux app (v1448.2) = `<home>/database/master.db` (SQLite catalog `book`/`author`) + per-book `database/book/<id%1000:03d>/<id>.db` (`page(id,part,page,number,services)` + `title(id,page,parent)`) + **Lucene** `store/` — page/title TEXT lives in Lucene (per-book SQLite has no text column); `PageText.body()` reads `getFields('page',['body','foot','comment'],"{book}-{page}")`, titles read `getField('title','body',"{book}-{title}")`. Write API = app's own `engine.Book(book_id).updatePage/updateTitle` (doc id `"{book}-{page}"`, fields `body`(TEXT)/`m_body`+`n_body`(ANALYSE)). |
| 2026-08-14 | **ID-space discovery.** The golden `shamelaID` is NOT the modern `book_id` — the two catalogs renumbered (golden روضة=103, modern=499; golden حصان طروادة=1791, modern=13682; golden فقه السنة=6451, modern=9486). ID overlap is coincidental. Migration must map golden book → modern book_id by **title matching**; golden books absent from modern master.db (~24k) need catalog entries added. |
| 2026-08-14 | **PoC converter built and validated.** `tools/convert_poc.py` runs headless in `dev-env/.venv` (Python 3.7.12) using the app's bundled JRE (`app/linux/64/jre/2`) + Lucene jars (`app/lucene/2`) via jpype1 + `access_parser`. Uses `d3/` (clean decompile) with `decompiled/` fallback in `sys.path`; stubs `dbmanager.CoreDb.sorter`. Converts: read `.mdb` → cp1256 decode (access_parser returns latin1-mojibake str; re-encode latin1→cp1256) → renumber duplicate title ids + remap parents → derive `title.page` by startswith-matching heading text vs page text → write per-book sqlite (DDL from `connectBook`) → write Lucene via `engine.Book` → `UPDATE master.db SET major_ondisk=pagecount`. Converted **499** (روضة الطالبين, 4,941pp/12 parts/1,174 titles), **13682** (حصان طروادة, 646pp), **9486** (فقه السنة, 2,120pp/3 parts/1,111 titles). All verified reading back through the app's own engine (proper Arabic, parts, TOC jumps). App launched live and stayed stable. Cleanup tool: `/tmp/opencode/cleanup.py` (delete by `Term('book_key', id)`). |
