# Session Log

> Part of shamela-linux/.project-knowledge/ | Last updated: 2026-08-18
> Append-only — never edit past entries.

| Date | Summary |
|------|---------|
| 2026-08-14 | Extracted the PyInstaller-frozen Shamela Linux 1448.2 binary (main `shamela.pyc` + `PYZ-00.pyz` -> ~250 pyc), decompiled all 39 app modules with uncompyle6 into `/tmp/opencode/decompiled/`. |
| 2026-08-14 | Created `/tmp/opencode/d3/` — cleaned working tree for the first 8 modules (shamela, updater, options, customs, displaybook, engine, ignore, searchboxes). |
| 2026-08-14 | Fixed all 6 `--- This code section failed: ---` decompilation failures + 2 broken f-strings in `d3/`: `detect_running_arch`, `prepareProgress`, `_settingsSnapshot`, `_changedPreviewKeys`, `realResolutions_new`, `ReadersBrowser.keyPressEvent`, `_buildHtmlFromParagraphs` (x2). All verified byte-identical to the original `.pyc` via `co_code` comparison; whole tree parses clean; zero failure markers remain. |
| 2026-08-14 | Persisted work artifacts into the repo (see history): copied `d3/`, `decompiled/` (39 modules), and `shamela-extract/` (289 pyc) from `/tmp/opencode`, verified integrity. Created `.project-knowledge/`. |
| 2026-08-14 | New goal set: mount the user's Windows Shamela database in the Linux build. Initial research notes added to roadmap. Repo committed + pushed. |
| 2026-08-14 | **DB-mount investigation.** Confirmed it is NOT a mount — the two formats are structurally incompatible -> it is a **migration**. Golden shamela = legacy Shamela 3.x: `Books/<id%10>/<id>.mdb` (Jet/Access, tables `book(id,part,page,nass,seal)` + `title(id,lvl,sub,tit)`, text cp1256) + SQLite `book_index.db` catalog (29,278 books, `shamelaID` stable id). Modern Linux app (v1448.2) = `<home>/database/master.db` (SQLite catalog `book`/`author`) + per-book `database/book/<id%1000:03d>/<id>.db` + **Lucene** `store/` for page/title text. |
| 2026-08-14 | **ID-space discovery.** The golden `shamelaID` is NOT the modern `book_id` — the two catalogs renumbered. ID overlap is coincidental. Migration must map golden book to modern book_id by **title matching**; golden books absent from modern master.db (~24k) need catalog entries added. |
| 2026-08-14 | **PoC converter built and validated.** `tools/convert_poc.py` runs headless in `dev-env/.venv` (Python 3.7.12). Converted 499, 13682, 9486. All verified reading back through the app's own engine. |
| 2026-08-14 | **Full migration tool built** — `tools/migrate_full.py` (phases map/catalog/convert/all). Map: 6,678/29,278 = 22.8% title-matched. Catalog: 22,593 new books added (ids 200000+). Convert: InsertBook fast-path, batched Lucene commits, resumable via progress.db. |
| 2026-08-14 | **Full run launched** (background): catalog phase (~8 min) then convert (~29k books). 44 pre-existing on-disk books skipped by default. |
| 2026-08-14 | **Catalog phase complete**: 22,593 new books added (ids 200000+), 0 errors. master.db backed up to `master.db.bak`. |
| 2026-08-14 | **Convert phase running** (PID recorded in convert.log, nohup): 29,236-book todo, ~1.3s/book steady state. Batched Lucene commits every 20 books; crash-safe resume via progress.db. |
| 2026-08-18 | **Convert phase COMPLETE.** 29,276/29,278 golden books converted (2 had zero pages, skipped). Full migration of the golden Shamela library into the modern Linux app is done. |
| 2026-08-19 | **Blank-rows bug found and fixed.** The migration wrote the `authors` column wrong for ALL 22,598 migrated books — 11,209 empty, 11,389 containing author NAMES or `-` instead of numeric IDs. The app's `fillBookCache` (`dbmanager.py:1958`) does `int(author.strip())` on that column → `ValueError` per row → completely blank, unclickable rows in the library. Fixed live: `UPDATE book SET authors = CAST(main_author AS TEXT) WHERE book_id >= 200000` (backup `master.db.bak2`), after verifying every affected book has exactly one author (`author_book` match). Also fixed the root cause in the repo: `tools/migrate_full.py` catalog phase now writes `authors_text = str(aid)` instead of the golden author *name*. Spot-checked 400 books against golden `book_index.db`: authors align (remaining diffs are name-order/kunya variants, e.g. `'الذهبي، شمس الدين'` vs `'شمس الدين الذهبي'`). App restarted → rows render correctly. |
| 2026-08-19 | **Live binary patching solved.** Goal: user-facing app tweaks (start-screen background image, slow startup, slow books tab) on the installed PyInstaller binary with NO source rebuild. Decoded the full storage layout: CArchive cookie at `0x414277` (`MEI\x0c\x0b\n\x0b\x0e`, archive_len 4,227,019, toc at `cookie_pos − toc_length` = 480 bytes, data base `archive_start + 88`), PYZ header 17 bytes, PYZ toc = marshal dict `{name:(typecode,offset,len)}` at `data[toc_offset:]`, entries zlib-compressed marshal code objects, pymagic byte-swapped (`42 0d 0d 0a` — keep original bytes). Delivery = **rebuild PYZ, splice in place, keep total exe size identical** (doesn't touch cookie/TOC/pydata/ELF headers). |
| 2026-08-19 | **First patch & crash loop debugged.** Injected `import shamela_patch; shamela_patch.install()` into `across` (first app import) + added the module to the PYZ. First boot: `TypeError: an integer is required (got type NoneType)` at across.py:5. Root cause (two compounding bytecode bugs): 3.6+ instructions are 2 bytes incl. `POP_TOP` (`01 00`), and `IMPORT_NAME` pops `fromlist` (top) then reads `level` beneath — compile order is `LOAD_CONST <0>` then `LOAD_CONST <None>`; had them swapped → `level=None` → TypeError. After fix, app boots through JVM + Qt fine. Also learned `shamela.orig-bak` was post-surgery (not pristine) — pristine source = `shamela-extract/.../across.pyc` (pre-dates surgery) and current `shamela.orig-bak` is the true original after restore. |
| 2026-08-19 | **Interception via sys.modules shims.** Meta-path finder (appended `Hook2`) NEVER fires for frozen names — PyInstaller's `PyiFrozenImporter` sits ahead in `meta_path` and wins. Rewrote `shamela_patch.py` to pre-register proxy shims in `sys.modules` (consulted before any finder); shim `__getattr__` triggers the real frozen import behind the scenes, then patches. Baseline boot verified: dbmanager/cache/mainwindow all intercepted and patched; `MainWindow.__init__` ~0.05–0.08s, `show` <0.01s. |
| 2026-08-19 | **Patch features landed (FIXED mode).** (1) Background override: `showBackground` loads external file — lookup `$SHAMELA_BG` → `bin/background.jpg|png` → `~/.shamela/background.jpg|png` (user dropped `background.jpg` in bin, verified in boot log). (2) Books tab: `BookCache._getCache` batch-prefill — 2 SQL queries (all books + all authors) replicate `fillBookCache` exactly (arabize, per-book_type icon paths incl. special icons, `(ت year)` deaths, joinAuthors, 9-tuple shape) vs ~31k per-book queries; re-prefill guard if cache resets. (3) Timing wrappers everywhere, log to `/tmp/shamela_boot.log`; `SHAMELA_BASELINE=1` = measure-only. Patched binary active at `~/Apps/shamela/app/linux/64/bin/shamela` (orig at `shamela.orig-bak`). |
| 2026-08-19 | **Patch tooling persisted.** Copied `surgery.py` / `shamela_patch.py` / `pyz_tool.py` + reapply README from volatile `/tmp/opencode/` into repo `patch/` and committed (with .project-knowledge updates). Reinstall flow documented: run surgery → swap → verify log. User's remaining asks: confirm books-tab speedup in real session; final background swap confirmation. |
